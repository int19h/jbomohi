# mail/wikichanges thread b8392bd3230b-wiki_page_writing_conventions_changed_by_eimi | root n32781.tdcrs7@www.lojban.org | 1 messages | rendered by jbomohi mail-v1

=== 1 | 2014-03-26T19:52:49Z | Apache <apache@digitalkingdom.org> | n32781.tdcrs7@www.lojban.org | mail/wikichanges/cur/1395863569.b8392bd3230bfb98.jbomohi:2,S
The page Writing conventions was changed by Eimi at 14:52 CDT

You can view the page by following this link:
 
http://www.lojban.org/tiki/Writing%20conventions

You can view a diff back to the previous version by following this link: 
http://www.lojban.org/tiki/tiki-pagehistory.php?page=Writing%20conventions&compare=1&oldver=1&newver=2


***********************************************************
The changes in this version follow below, followed after by the current full page text.
***********************************************************


+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+
@@ -Lines: 5-9 changed to +Lines: 5-9 @@
+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+
* ?{…} is used for questionable text (either a subject of debate, or to quote without asserting correctness, for instance, asking a student what, if anything, the text would mean). 
* !{…} is used for text that parses, but has a surprising or wrong meaning (one not intended).
- * [] is used to indicate words (usually terminators or {cu}) that can be included or omitted without changing the parse.
+ * ~np~[]~/np~ is used to indicate words (usually terminators or {cu}) that can be included or omitted without changing the parse.
* -…- is used for quoting rafsi, like -mib-, the rafsi for {mi}
* "…" is used for quoting English. The *, ?, and ! modifiers can be applied if needed.




***********************************************************
The new page content follows below.
***********************************************************

Some conventions used when talking about Lojban in English, for instance on IRC or in mailing lists. At least, these are the ones that I (xalbo) usually use.



* {…} is used to quote Lojban text.

* *{…} is used for incorrect Lojban text. 

* ?{…} is used for questionable text (either a subject of debate, or to quote without asserting correctness, for instance, asking a student what, if anything, the text would mean). 

* !{…} is used for text that parses, but has a surprising or wrong meaning (one not intended).

* ~np~[…]~/np~ is used to indicate words (usually terminators or {cu}) that can be included or omitted without changing the parse.

* -…- is used for quoting rafsi, like -mib-, the rafsi for {mi}

* "…" is used for quoting English. The *, ?, and ! modifiers can be applied if needed.

* ⟨…⟩ is used for quoting sequences of characters. "No, you couldn't use ⟨la⟩ in a cmevla pre-Dot Side."

* † is used to qualify a statement, to mean "This isn't actually true, but it's close enough for the current purposes." "cmevla can only† be used as names, but not all names are cmevla."

* ‡ is used for "I don't know whether this is actually precisely true or not, but it's close enough for now." "You use ZOI, then any‡ Lojban word as a delimiter…" (Really? What about {bu}? What about {si}? What about {fa'o}?)


_______________________________________________
Wikichanges mailing list
Wikichanges@lojban.org
http://mail.lojban.org/mailman/listinfo/wikichanges

