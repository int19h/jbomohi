These are all developed in SWI-Prolog. Prolog implementations are not all that different, but I make no guarantees for portability. --((tsali))

{ATTACH(name=>lojban.pl,showdesc=>1)}{ATTACH}
{ATTACH(name=>lojban-vocab.pl,showdesc=>1)}{ATTACH}
{ATTACH(name=>lujvoprn.pl,showdesc=>1)}{ATTACH}