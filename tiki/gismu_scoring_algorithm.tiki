Contains an implementation in Python of the gismu scoring algorithm as discussed in CLL 4.14

(Or will, once Broca figures out why he can't upload it.)

~tc~
(alias(gismu creation algorithm))
~/tc~